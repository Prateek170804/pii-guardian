# OPTIONAL access protection.
# To require a password to open the app, copy this file to "secrets.toml" in the
# same folder and set a value. (Alternatively set the PII_GUARDIAN_PASSWORD env var.)
# Do NOT commit secrets.toml to version control.
password = "change-me"
